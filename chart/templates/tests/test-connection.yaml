apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "regcred-injector.fullname" . }}-test-connection"
  labels:
    {{- include "regcred-injector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "regcred-injector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
